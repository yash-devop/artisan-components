<h1 className="text-3xl font-semibold">Code Block</h1>

<h1 className="py-4 text-neutral-500">

The Code Block component is a composable, syntax-highlighted container for displaying source code in documentation and developer tooling.

It is designed to handle real-world code snippets, not just static examples — including multiple files, copy-to-clipboard, highlighted lines, and horizontal scrolling.

</h1>

import { ComponentSource } from "@repo/ui/component-preview";
import TabsCode from "@repo/primitives/srcDir/tabs/tabs.tsx?raw";

<ComponentSource title="tabs.tsx" code={TabsCode} />
