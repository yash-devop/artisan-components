<h1 className="text-3xl font-semibold">Package Installer</h1>

<h1 className="py-4 text-neutral-500">
  The Package Installer component lets you display install commands for
  different package managers like npm, yarn, pnpm, and bun—with a simple,
  tab-based UI.
</h1>

import { SquareTerminal, Activity } from "lucide-react";
import {
  BlockRoot,
  BlockContent,
  BlockCopy,
  BlockHeader,
  BlockIcon,
  BlockList,
  BlockTab,
} from "@repo/ui/code-block";
import { ComponentSource } from "@repo/ui/component-preview";

<div className='space-y-6'>
  <BlockRoot defaultOpen="npmsds">
    <BlockHeader>
      <BlockIcon>
        <SquareTerminal
          size={20}
          className="stroke-1 text-neutral-600 rounded-none shrink-0 mt-0.5 "
        />

      </BlockIcon>
      <BlockList>
        <BlockTab value="npm">npm</BlockTab>
        <BlockTab value="pnpm">pnpm</BlockTab>
        <BlockTab value="bun">bun</BlockTab>
      </BlockList>

      <BlockCopy />
    </BlockHeader>
    <BlockContent value="npm">
      npx shadcn@latest add @eldoraui/clerk-otp
    </BlockContent>
    <BlockContent value="pnpm">
      pnpm dlx shadcn@latest add @eldoraui/clerk-otp
    </BlockContent>
    <BlockContent value="bun">
      bunx --bun shadcn@latest add @eldoraui/clerk-otp
    </BlockContent>

</BlockRoot>

</div>
